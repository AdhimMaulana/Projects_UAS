<?php 
    
    $hostname   = "localhost"; 
    $username   = "root"; 
    $password   = ""; 
    $database   = "apk-musik"; 

  
    $db   = mysqli_connect(
                $hostname,
                $username,
                $password,
                $database);
?>