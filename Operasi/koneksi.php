<?php 
    
    $hostname   = "localhost"; 
    $username   = "root"; 
    $password   = ""; 
    $database   = "mv_db"; 

  
    $conn   = mysqli_connect(
                $hostname,
                $username,
                $password,
                $database);
?>